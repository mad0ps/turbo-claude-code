# Tools Instincts

<!-- Format: confidence | trigger → action (source, date) -->
0.8 | content needed sometimes not always → use skills (lazy-load) instead of rules (always-load) (pattern, 2026-03-28)
0.7 | plugins not used daily → disable in settings, re-enable on demand (decision, 2026-03-28)
